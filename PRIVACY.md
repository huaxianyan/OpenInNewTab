# 隐私说明

OpenInNewTab 只处理用户主动配置的网站和链接规则。

## 收集的数据

扩展不收集、上传或出售个人数据，不包含统计、广告和行为追踪代码，也不向开发者服务器发送浏览记录。

## 保存的数据

扩展使用 `chrome.storage.sync` 保存用户创建的规则和用户选择的规则上游地址，包括：

- 规则名称
- 网站适用范围
- 链接选择范围
- 排除范围
- 打开方式和启用状态
- 用户选择的规则上游地址

这些数据由 Chrome 的扩展存储机制管理。用户可以在扩展设置中删除规则，也可以通过卸载扩展清除扩展数据。

## 网站访问权限

扩展不会在安装时请求访问全部网站。只有用户为某个网站创建规则时，扩展才会请求该网站的访问权限。

获得权限后，扩展仅用于：

- 识别用户配置的链接
- 在新标签中打开匹配链接
- 在配置过程中高亮预览匹配和排除范围

## 规则上游访问

用户可以主动从规则上游读取社区规则，也可以把上游地址改为自己信任的 HTTP 或 HTTPS 地址。扩展仅在用户点击获取时访问该地址及其索引列出的同站点规则文件，不会后台轮询。请求不携带网站 Cookie，也不会向上游发送用户已有的规则或浏览记录。

默认上游是本项目 GitHub 仓库中的规则目录。访问上游时，上游服务提供者会像处理普通网络请求一样获得连接所需的信息，例如 IP 地址和请求时间。用户在导入前可以查看规则并取消不需要的项目。

上游只能提供结构化 JSON 规则。扩展不会把规则作为代码执行，也不加载远程 JavaScript 或 CSS。

用户点击普通网页链接后产生的页面访问由浏览器和目标网站处理。

## 联系方式

如对隐私说明有疑问，请通过 GitHub Issues 联系：

https://github.com/huaxianyan/OpenInNewTab/issues
