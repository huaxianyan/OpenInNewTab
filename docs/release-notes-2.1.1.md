# OpenInNewTab 2.1.1

## 新功能

- 访问已有规则但当前设备尚未授权的网站时，显示一次浏览器通知
- 点击通知进入设置页，可集中授权所有启用规则缺少的网站权限
- 每条规则分别显示「授权网站」或「已授权」，支持单独授权
- 在目标网站打开扩展弹窗时，只在缺少权限的情况下显示当前网站授权入口
- 完成当前网站授权后立即加载规则，无需重新导入或保存

## 同步行为

- 规则和启用状态继续通过 Chrome 同步
- 网站访问权限由 Chrome 保存在当前设备，仍需用户在每台设备上确认
- 缺少本机权限不会停用同步规则，也不会影响其他设备
- 浏览器通知按网站在当前会话中去重，刷新页面不会重复提醒

## 权限和隐私

- 新增 `webNavigation`，仅在本机将顶层页面地址与用户已有规则比较
- 新增 `notifications`，仅在命中规则但当前设备缺少权限时显示提醒
- 页面地址不会因此写入同步存储，也不会发送给开发者或第三方
- 网站权限仍按需申请，安装时无需授予全部网站访问权限

## 发布文件

- 文件：`OpenInNewTab-2.1.1.zip`
- SHA256：`0EE31E997C9BCB013F7168AD8D120688CA9D56F6FB81FDFB7EBF12F10368C9BD`

## 验证状态

- JavaScript 语法检查通过
- 9 项模块测试全部通过
- 发布包包含 14 个运行文件，包内文件与源码哈希一致
- Edge 干净 Profile 自动验收通过，包括待授权规则的真实顶层页面导航
- 桌面通知、授权按钮和官方 Chrome 最终流程仍需按发布清单人工验收
