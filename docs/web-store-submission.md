# Chrome Web Store 提交资料

## 基本信息

- 扩展名称：新标签打开规则
- 默认语言：简体中文
- 类别：生产力工具
- 官方主页：https://github.com/huaxianyan/OpenInNewTab
- 支持页面：https://github.com/huaxianyan/OpenInNewTab/issues
- 隐私政策：https://github.com/huaxianyan/OpenInNewTab/blob/main/PRIVACY.md
- 发布包：`dist/OpenInNewTab-2.1.1.zip`
- SHA256：`0EE31E997C9BCB013F7168AD8D120688CA9D56F6FB81FDFB7EBF12F10368C9BD`

用户已确认 2.0.0 审核通过。本次为 2.1.1 更新，上传时选择已通过审核的原有 Web Store 条目，不要创建新条目。

## 简短说明

按网站设置规则，让指定链接在新标签中打开。支持页面内可视化选择、排除链接和保护网站原有导航。

## 详细说明

有些网站会让普通左键点击始终在当前页面打开，或者通过脚本接管链接行为。新标签打开规则允许你按网站选择需要处理的链接，并保留其他链接原有的打开方式。

主要功能：

- 在页面上直接选择同类链接，无需手写规则
- 扩大或缩小匹配范围，并即时预览结果
- 添加多条排除项，单独查看每条排除项负责的链接
- 在兼容模式和保护模式之间切换
- 从扩展图标快速启停、编辑或删除当前网站规则
- 导入、预览、按项选择和导出规则
- 主动获取云端社区规则，在弹窗中浏览并选择导入
- 自定义规则来源，或恢复默认来源
- 规则同步到其他设备后，提醒并集中完成该设备的网站授权
- 支持动态生成的链接和单页应用页面

扩展安装时无需授予全部网站的访问权限。为网站创建或导入规则时，浏览器会请求对应网站的访问权限。主动获取云端规则时，还会请求规则来源网站的访问权限。Chrome 不会在设备间同步网站权限；用户访问已有规则但本机尚未授权的网站时，扩展会显示一次浏览器通知，并提供当前网站、单条规则或集中授权入口。

兼容模式保留网站原有点击处理。保护模式用于网站接管普通点击的情况，它会阻止网站处理该次点击，再由浏览器执行链接原生导航。真实链接地址始终保留，因此中键、右键菜单和复制链接仍可正常使用。

扩展不包含广告、统计或行为追踪，不向开发者服务器发送浏览记录，也不加载远程代码。

## 单一用途

允许用户按自己配置的网站和链接规则，在新标签中打开指定链接。

## 权限说明

### `activeTab`

用户点击扩展图标后，临时访问当前标签页，用于识别当前网站并启动页面内可视化链接选择。扩展不会依靠此权限在用户未操作时访问其他标签页。

### `notifications`

规则通过 Chrome 同步到其他设备时，网站访问权限不会一同同步。用户访问已有规则但本机尚未授权的网站时显示一次浏览器通知，引导用户前往设置完成授权。通知按网站在当前浏览器会话中去重。

### `scripting`

在用户已经授权的网站中注入链接规则处理脚本和可视化选择界面。脚本只用于匹配、预览和处理用户配置的链接。

### `storage`

使用 Chrome 扩展存储保存和同步用户创建的规则，包括网站范围、链接范围、排除范围、打开方式和启用状态，以及用户选择的规则来源地址。会话存储仅记录本次浏览器会话中已经提醒过的网站，防止重复通知。开发者无法访问用户的 Chrome 同步存储。

### `webNavigation`

在本机读取顶层页面的导航地址，与用户已经保存的启用规则进行匹配。仅当页面命中规则且本机缺少对应网站权限时触发授权提醒。导航地址不会因此保存到同步存储，也不会发送给开发者或第三方。

### 可选网站权限 `*://*/*`

这是可选权限声明，不在安装时授予。扩展适用于用户自行选择的网站，因此需要允许用户在运行时申请任意 HTTP 或 HTTPS 网站。实际使用时请求用户当前创建或导入规则所涉及的网站，以及用户主动获取云端规则所需的来源网站。删除网站最后一条规则后，若该权限也未被当前规则来源使用，会撤销对应权限。更换规则来源后，也会清理不再使用的旧来源权限。

## 数据使用披露

建议在「收集的用户数据」中选择不收集用户数据。扩展会在本地读取顶层页面 URL，用于判断已有规则是否缺少本机权限；获得网站权限后会读取匹配的链接元素以执行规则。网站内容、页面地址、浏览记录和规则都不会发送给开发者或第三方服务器。

规则和规则来源地址通过 `chrome.storage.sync` 由 Chrome 管理。开发者没有服务器，也不能读取用户的同步数据。

用户主动获取云端规则时，扩展访问所选来源的索引及同站点规则文件。默认来源托管于 GitHub。请求不携带网站 Cookie，也不发送用户已有规则或浏览记录。来源服务提供者会接收到 IP 地址和请求时间等普通连接信息。请按此实际行为核对后台数据使用披露，并保留新版隐私政策中的网络访问说明。

需要确认以下声明：

- 不向第三方出售用户数据
- 不将用户数据用于与扩展单一用途无关的目的
- 不将用户数据用于信用评估或借贷
- 不使用或转移用户数据投放个性化广告
- 不包含分析、广告或追踪 SDK

## 远程代码声明

扩展不执行远程代码。所有 JavaScript 和 CSS 均包含在上传的扩展包中。云端规则是结构化 JSON 数据，仅描述网站范围、链接选择器、排除项和预定义打开模式。扩展仅在用户主动获取时联网读取，不会后台轮询或自动启用，也不会把规则作为代码执行。

## 审核员说明

2.0.0 已通过审核。本次 2.1.1 新增云端规则浏览、自定义规则来源，以及跨设备同步后的本机授权流程，并修正 PSNINE 分页链接的排除范围。

本次新增 `notifications` 和 `webNavigation` 权限。Chrome 不会同步可选网站权限；`webNavigation` 仅在本机将顶层页面地址与用户已有规则比较，`notifications` 仅在命中规则但本机缺少权限时显示授权提醒。页面地址不会发送到外部。

验证步骤：

1. 安装扩展，确认安装时不请求访问全部网站
2. 打开 `https://www.v2ex.com/`
3. 点击扩展图标，选择「选择页面上的链接」
4. 允许访问 `www.v2ex.com`
5. 点击一个主题链接，确认相似主题链接显示蓝色预览
6. 保存规则，普通点击主题链接后应在新标签中打开
7. 再次点击扩展图标，可以启停、切换模式、可视化编辑或删除规则
8. 删除该网站最后一条规则后，对应网站权限会被撤销

云端规则验证步骤：

1. 点击扩展图标，再点击「管理全部规则」进入设置页
2. 使用默认规则来源，点击「获取云端规则」并允许访问来源网站
3. 在云端规则弹窗中选择一个规则集，点击「查看并选择」
4. 预览并勾选需要的规则，确认导入并允许访问目标网站
5. 导入后返回云端规则列表，可继续选择其他规则集
6. 刷新目标网站，确认导入规则生效

同步授权验证步骤：

1. 在设置页保留一条启用规则，然后在扩展详情页撤销对应网站权限
2. 打开该规则适用的网站，确认浏览器显示授权提醒；同一浏览器会话中刷新页面不会重复提醒
3. 点击通知进入设置页，确认待授权规则显示「授权网站」，已授权规则显示「已授权」
4. 使用「集中授权」一次申请全部启用规则缺少的网站权限
5. 再次撤销当前网站权限并点击扩展图标，确认 Popup 只显示当前网站的授权入口；允许后当前页面规则可用

扩展不需要账号。默认规则来源为本项目 GitHub 仓库，用户也可以选择自己信任的 HTTP 或 HTTPS 来源。

## 发布前后台确认

- [ ] 选择原有扩展条目
- [ ] 确认原扩展 ID
- [x] 2.0.0 已通过审核（用户确认）
- [ ] 完成 2.1.1 官方 Chrome 最终验收
- [ ] 上传 `OpenInNewTab-2.1.1.zip`
- [ ] 填写简短说明和详细说明
- [ ] 填写单一用途
- [ ] 填写全部权限说明
- [ ] 选择不收集用户数据，并完成数据使用声明
- [ ] 填写隐私政策 URL
- [ ] 填写主页和支持 URL
- [ ] 检查现有商店图标和截图，按需补充云端规则截图
- [ ] 填写审核员说明
- [ ] 保存草稿并检查所有警告
- [ ] 提交审核

参考：

- https://developer.chrome.com/docs/webstore/publish
- https://developer.chrome.com/docs/webstore/cws-dashboard-privacy
- https://developer.chrome.com/docs/webstore/program-policies/policies
